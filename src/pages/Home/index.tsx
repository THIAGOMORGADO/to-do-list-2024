import { Text, View } from "react-native"

import { Container, Title } from './styled';

export const Home = () => {
    return (
        <Container>
            <Title>Tela Home</Title>
        </Container>
    )
}