import { Text, View } from "react-native"

import { Container } from './styled';

export const Home = () => {
    return (
        <Container>
            <Text>Tela Home</Text>
        </Container>
    )
}